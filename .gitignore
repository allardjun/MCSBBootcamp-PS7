# The .venv and quarto's scratch directory are the only build products here.
# The .ipynb is committed on purpose: edits can be proposed against either it or the .qmd it is rendered from.
.venv/
.quarto/
.ipynb_checkpoints/
