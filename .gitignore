# Everything here is generated by make_ipynb.sh or by uv.
# The .qmd is the source; the .ipynb is a build product, so that edits are proposed against one file rather than two.
.venv/
.quarto/
*.ipynb
.ipynb_checkpoints/
